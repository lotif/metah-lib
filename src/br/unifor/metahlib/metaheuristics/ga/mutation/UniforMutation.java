package br.unifor.metahlib.metaheuristics.ga.mutation;

public class UniforMutation {
/*
     private FuncaoObjetivoNumerica funcao;

    public MutacaoUniforme( FuncaoObjetivoNumerica funcao ){
        this.funcao = funcao;
    }

    @Override
    public boolean mutar(Object[] genes, double pm) {
        double variacao = funcao.x_max - funcao.x_min;
        double valor;
        boolean mutou = false;
        for ( int i = 0; i < genes.length; ++i ){
            if ( random.nextDouble() < pm ){
                valor = (random.nextDouble() * variacao) + funcao.x_min;
                genes[i] = funcao.limitarX(valor);
                mutou = true;
            }
        }

        return mutou;
    }

 */
}
