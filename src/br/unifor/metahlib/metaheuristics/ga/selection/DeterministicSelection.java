package br.unifor.metahlib.metaheuristics.ga.selection;

public class DeterministicSelection {
	
	/*
		public Individuo[] selecionar( Individuo[] individuos, int qtdIndividuos ){
	        individuos = individuos.clone();
	        Arrays.sort( individuos );
	        Individuo[] selecao = new Individuo[qtdIndividuos];
	        for ( int i = 0; i < selecao.length; ++i ){
	            selecao[i] = individuos[ individuos.length - i - 1];
	            assert( i == 0 || selecao[i-1].getFitness() >= selecao[i].getFitness());
	        }

	        return selecao;
	    }
	*/

}
