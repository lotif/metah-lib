package br.unifor.metahlib.metaheuristics.ga.mutation;

public class GaussianMutation {
/*
    private FuncaoObjetivoNumerica funcao;

    public MutacaoGaussiana( FuncaoObjetivoNumerica funcao ){
        this.funcao = funcao;
    }

    @Override
    public boolean mutar(Object[] genes, double pm) {
        double desvioPadrao = 1; // funcao.x_max - funcao.x_min;
        double valor;
        boolean mutou = false;
        for ( int i = 0; i < genes.length; ++i ){
            if ( random.nextDouble() < pm ){
                valor = (random.nextGaussian() * desvioPadrao) + (Double) genes[i];
                genes[i] = funcao.limitarX(valor);
                mutou = true;
            }
        }

        return mutou;
    }

 
 */
}
